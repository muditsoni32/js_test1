{
  "name": "jest-junit-reporter",
  "version": "1.1.0",
  "description": "A JUnit test report formatter for Jest.",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [
    "jest",
    "junit",
    "reporter"
  ],
  "author": {
    "name": "Mudit Soni",
    "email": "muditsoni32@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/muditsoni32/js_test1.git"
  },
  "license": "MIT",
  "dependencies": {
    "ansi": "^0.3.1", // Ensure this version exists and is compatible
    "xml": "^1.0.1", // Check latest or more compatible versions
    "libxml-xsd": "^0.5.0" // Look for alternatives or check if a compatible update exists
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "node-gyp": "^9.0.0" // Updated to a version that is likely compatible with Node.js 18
  },
  "jest": {
    "testEnvironment": "node",
    "testRegex": "(((\\.|/)(spec))\\.js?$)"
  }
}
